/target
.DS_Store
*.swp
*.swo
*~
*.bin

# The default SQLite store and its sidecars. The trailing-slash forms below
# are the older TidesDB directory layout; SQLite writes a plain file, which
# those patterns do not match.
/memory.mcpmem/
*.mcpmem/
*.mcpmem
*.mcpmem-wal
*.mcpmem-shm

# Local design notes, plans, and reviews (not for the published crate)
PERF_OPTIMIZATION_PLAN.md
OPTIMIZATION_PLAN.md
REVIEW_MEMORY_IO_CONCURRENCY.md
new arch plan.md
examples

# Subagent-driven development scratch (ledger, briefs, review packages)
.superpowers/
.worktrees/
